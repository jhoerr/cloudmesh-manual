Commands
========

EOF        data     image      network  secgroup  stopwatch  vcluster
admin      default  info       open     set       storage    vdir    
banner     echo     inventory  pause    shell     sys        version 
clear      flavor   key        plugin   sleep     url        vm      
config     group    login      q        source    var        workflow
container  help     man        quit     ssh       vbox     

